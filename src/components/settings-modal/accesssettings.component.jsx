import React from "react";
import Box from "@mui/material/Box";

export default function AccessSettings({ uaManager }) {
    return (
        <Box sx={{ width: 200 }}>
            <h1>No Access Settings Yet</h1>
        </Box>
    );
}
