export default function contentURL(suffix) {
    //return "http://localhost:5000/" + suffix;
    //return "https://canvas-server.onrender.com/" + suffix;
    return "https://canvas-server.adaptable.app/" + suffix;
}
